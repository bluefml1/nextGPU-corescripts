s.txt
**/user-s3.env
**/*user-s3*.env
!**/user-s3.env.example
**/ondemand-gpu-host.env
!**/ondemand-gpu-host.env.example
**/bin/
**/obj/
apps/NextGPU/publish/*
!apps/NextGPU/publish/README.txt
!apps/NextGPU/publish/.gitkeep
/NextGPU.exe
/.dotnet/

# Runtime logs (keep folder placeholder)
*.log
logs/*
!logs/.gitkeep

# Host-specific / machine local
/machine-profile.json
/domain.txt
**/*.path
!**/*.path.example
**/*.paths.json

# Local / stray binaries (do not ship)
/cloudflared.exe
apps/NextGPU/test_copy.exe

# Large driver package drop-in (download/install separately)
/VDD-VAD-Install/

# Vendor / third-party drop-ins
/nssm/
/sunshine/
/moonlight-web/

# Misplaced duplicate app trees (canonical is apps/NextGPU/*)
/apps/NextGPU.App/
/apps/NextGPU.Core/

# Local Playnite database dumps
/library/

# SteamExtensions/.cache is download-only; committed build/ is required for setup
# PlayNiteWatcher/SteamExtensions/build/  # kept in repo (SteamLibrary_NextGPU)
PlayNiteWatcher/SteamExtensions/.cache/

# Local PDF export tooling (md-to-pdf)
docs/node_modules/
docs/package-lock.json
/docs/package.json
